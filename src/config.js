// Configuration variables for the educational video generator.
// Changing these values will automatically rebuild the script, timeline,
// narration, and visual scenes without modifying other source files.

export const TARGET_VIDEO_DURATION = 15; // Target duration in minutes (e.g. 8 or 15)
export const WORDS_PER_SECOND = 2.48;    // Calibrated speaking speed for 15:00-15:30 target
